#include "main.h"
#include <stdio.h>

/**
 * _abs - compute the absolute value of an intiger
 *@t: the value to be inputed
 * Return: Always 0.
 */
int _abs(int t)
{
	if (t >= 0)
		return (t);
	else
		return (-t);
}
